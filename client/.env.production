# Production API — used when you run `npm run build` / Vercel frontend deploy
# If your backend URL is different, change it to the domain shown under the backend project → Domains
VITE_API_URL=https://noor-ai-project-abop.vercel.app
